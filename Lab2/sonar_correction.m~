function [x_real, y_real , sonar ] = sonar_correction (x,y,x_ref,y_ref,j)
%sonar_correction: correct odometry using sonars
%   Detailed explanation goes here

sonar = pioneer_read_sonars;

% correction in x (1st and 3rd corridor)
% two sonar area
if y > 5.7 && y < 6.7 && j == 5
    desvio = (sonar(1) + sonar(8))/2;
    x_real = x_ref + desvio;
    y_real = y_ref;
elseif
    x_real = 
end

end

